:PROPERTIES:
:GPTEL_PRESET: default
:GPTEL_MODEL: haiku
:GPTEL_BACKEND: Claude Code
:GPTEL_SYSTEM:
:GPTEL_TOOLS:
:END:
#+title: B3 - Subagent, Abort, Error Tests (7-9)

Prep: =M-: (setq gptel-log-level 'debug)=

*Test 7 — Subagent inline output*
1. Send from =* Test 7= heading
- PASS: Claude Code spawns a subagent via Task tool. Subagent output appears inline in the response. Stream events with parent_tool_use_id handled. No errors in =*Messages*=.

*Test 8 — gptel-abort on live process*
1. Send from =* Test 8= heading (long prompt → long response)
2. While Claude is actively streaming text, run =M-x gptel-abort=
- PASS: Process killed. Buffer shows partial response (no corruption). No errors in =*Messages*=. Timeout timer cancelled. File watchers cleaned up. Check =M-x list-processes= to confirm no lingering gptel-claude-code process.

*Test 9 — Error result from Claude Code*
Before sending, set fake session properties on the heading:
  =M-x org-set-property RET GPTEL_CLAUDE_CODE_SESSION RET fake-session-does-not-exist-12345=
  =M-x org-set-property RET GPTEL_CLAUDE_CODE_MODEL RET haiku=
1. Send from =* Test 9= heading (triggers --resume on nonexistent session)
- PASS: Error shown in buffer gracefully (not a crash). Process exits cleanly.
- ALT: If Claude Code creates new session instead of erroring, test binary-not-found:
  =M-: (setf (gptel-claude-code-claude-command (alist-get "Claude Code" gptel--known-backends nil nil #'equal)) "nonexistent-claude-binary")=
  Send, then restore: =M-: (setf (gptel-claude-code-claude-command (alist-get "Claude Code" gptel--known-backends nil nil #'equal)) "claude")=

* Test 7: Subagent inline output
@user: Use the Task tool to spawn a quick research agent that answers: "What is 2+2?" Show me the agent's full response.

* Test 8: gptel-abort on live process
@user: Write an extremely detailed, comprehensive history of computing from ancient abacuses through modern quantum computers. Cover every major figure, their contributions, technical details, and societal impact. Make it at least 3000 words with extensive detail on each era.

* Test 9: Error result from Claude Code
@user: Hello, this is an error handling test.
