:PROPERTIES:
:GPTEL_PRESET: default
:GPTEL_MODEL: haiku
:GPTEL_BACKEND: Claude Code
:GPTEL_SYSTEM:
:GPTEL_TOOLS:
:END:
#+title: B1 - Session Tests (1-3)

Prep: =M-: (setq gptel-log-level 'debug)=

*Test 1 — Multi-turn --resume*
1. Send from =* Test 1= heading (cursor after @user: line, gptel-send)
2. Wait for response ("Noted: pineapple")
3. After response, type: =@user: What was the secret word? Just the word.=
4. Send again
- PASS: Second response says "pineapple". =*Messages*= shows =--resume= in CLI args.

*Test 2 — Org branching --fork-session*
- Prerequisite: Test 1 must have completed (session stored on =* Test 1=)
1. Send from =** Test 2= heading
- PASS: =*Messages*= shows =--resume= and =--fork-session=. Response says "pineapple" (forked context).

*Test 3 — Model switching --fork-session --model*
1. Send from =* Test 3= heading (creates session with haiku)
2. After response, change model: =M-: (setq gptel-model 'sonnet)=
   (or use =C-c RET= gptel menu → change model to sonnet)
3. Type: =@user: What code word did I say? Just the word.=
4. Send again
- PASS: =*Messages*= shows =--fork-session= and =--model sonnet=. Only ONE =--model= flag.

* Test 1: Multi-turn --resume
@user: Remember the secret word "pineapple". Reply ONLY with: "Noted: pineapple"

** Test 2: Org branching --fork-session
@user: What was the secret word from our earlier conversation? Reply with just the word, nothing else.

* Test 3: Model switching --fork-session --model
@user: Remember the code word "mango". Reply ONLY with: "Noted: mango"
