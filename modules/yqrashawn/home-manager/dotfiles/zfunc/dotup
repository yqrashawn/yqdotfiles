dotup () {
  echo "Please enter root password"
  sudo echo "OK"
  pushd .
  cd ~/.nixpkgs/
  nix --extra-exterimental-features "nix-command flakes" develop --impure -c sysdo switch yqrashawn-intel
  popd
}
