#!/usr/local/bin/bb

(ns gh-interested-pr
  (:require
   [cheshire.core :as json]
   [lib.github.pr :as ghpr]
   [lib.github.core :as gh]))

(def intrested-repos
  [["Conflux-Chain" "helios"]])

;; (defn- interested-pr? [{:keys [user draft]}])

;; (doseq [[owner repo] intrested-repos]
;;   (->> (ghh/my-open-pr owner repo)
;;        (filter)))
